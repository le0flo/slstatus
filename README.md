# Status

Una fork di slstatus.

### Dipendenze

Dipendenze fondamentali per la compilazione:

```sh
sudo pacman -S base-devel xorg-server libx11
```

### Installazione

```sh
cd slstatus
sudo make clean install
```
